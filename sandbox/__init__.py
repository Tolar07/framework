"""Sandbox: pre-season machinery test on real live Club Friendlies.

QUARANTINE: sandbox legs live in their own ledger (sandbox/sandbox_log.json),
carry phase="sandbox", and never count toward the Phase-3 gate. The engine is
never fit on friendly results. NO friendly odds exist in the framework's
sources, so sandbox CLV is always NO DATA — PENDING: this tests the MACHINERY
(fixtures -> rating -> logging -> settlement -> brain), not edge.
"""
